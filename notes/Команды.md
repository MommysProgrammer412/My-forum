- poetry --version — Проверяет, установлен ли менеджер зависимостей Poetry и какой он версии.
- poetry run python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())" — генерация секретного ключа в терминале через poetry
- deactivate деактивация виртуального окружения
- poetry config virtualenvs.in-project true Настройка Poetry (чтобы он создавал окружение в папке проекта)
- poetry init --no-interaction Создание конфигурацию проекта (Флаг --no-interaction создаст файл pyproject.toml быстро с настройками по умолчанию)
- poetry add django Установка Django (Poetry сам создаст новую папку .venv)
- Invoke-Expression (poetry env activate) активация виртуального окружения через poetry
- poetry add python-dotenv установка библиоткеи для работы с переменными окружения (секретный ключ и тд; Эта команда сама обновит файлы pyproject.toml и poetry.lock, а также установит пакет в виртуальное окружение)
-
-
-
-
-
-
- python -m venv venv — Создает виртуальное окружение в папке venv (БЕЗ POETRY! НЕ НАДО!).
- python manage.py --version — Показывает версию самого Django, установленного в проекте (БЕЗ POETRY! НЕ НАДО!).
- python -c "import django; print(django.get_version())" — Альтернативный способ узнать версию Django через Python-код прямо в терминале (БЕЗ POETRY! НЕ НАДО!). 
- venv\\Scripts\\Activate.ps1 — активация виртуального окружения (PS; БЕЗ POETRY! НЕ НАДО!)
- poetry add django установка django в виртуальное окружение (БЕЗ POETRY! НЕ НАДО!)

